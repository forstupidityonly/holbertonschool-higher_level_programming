#!/usr/bin/python3
"""must use isinstance bc that checks for inheritence and it asked for that"""


def is_kind_of_class(obj, a_class):
    """stuff for checker"""
    return(isinstance(obj, a_class))

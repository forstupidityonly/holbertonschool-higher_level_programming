#include "lists.h"
/**
  *
  *
  */
listint_t *insert_node(listint_t **head, int number)
{
	listint_t *current, *last, *new;

	if (head == NULL)
		return (NULL);
	last = head;
	if (last == NULL || number < ['/j
}
